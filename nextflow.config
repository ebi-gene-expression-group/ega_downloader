params {
    dataDir = 'data'
    metadataDir = 'metadata'
    egaCredentialsDir = 'credentials'
}

process {
    executor='lsf'
    queue='production-rh74'
}

executor {
    queueSize=500
    perJobMemLimit=true
    exitReadTimeout='100000 sec'
    pollInterval = '5sec'
}
